public class BinarySearch {

    int size;
    int[] array;


    public BinarySearch(int size,
                        int[] array){
        this.size = size;
        this.array = array;
    }

    public void binarySearch(int[] array){

        // 중간값 설정
        // 중앙값과 검색값을 비교
        // 선택 구간에 대해서 중간값 결정
        // 중간값과 검색값을 비교
        // ( 중간값 == 검색값 ) 일때 , 검색 종료

    }
}
